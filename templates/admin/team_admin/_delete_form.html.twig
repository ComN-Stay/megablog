<form method="post" action="{{ path('app_team_admin_delete', {'id': team.id}) }}" onsubmit="return confirm('Supprimer cette donnée ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team.id) }}">
    <button class="btn btn-danger">Supprimer<i class="fa-solid fa-trash-alt fa-lg ml-3"></i></a>
</form>
