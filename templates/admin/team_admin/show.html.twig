{% extends 'admin/base_admin.html.twig' %}

{% block title %}Team{% endblock %}

{% block body %}
    <h1>Team</h1>

    <table class="table">
        <tbody>
    
            <tr>
                <th>Prénom</th>
                <td>{{ team.firstname }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ team.lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ team.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ team.roles ? team.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

<div class="d-flex">
    <a href="{{ path('app_team_admin_index') }}" class="btn btn-info mr-3"><i class="fa-solid fa-chevron-left fa-sm mr-3"></i>Retour</a>

    <a href="{{ path('app_team_admin_edit', {'id': team.id}) }}" class="btn btn-info mr-3">Editer<i class="fa-solid fa-pen-to-square fa-lg ml-3"></i></a>

    {{ include('admin/team_admin/_delete_form.html.twig') }}
</div>
{% endblock %}
