{% extends 'admin/base_admin.html.twig' %}


{% block body %}
    <h1>Administrateurs</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for team in teams %}
            <tr>
                <td>{{ team.firstname }}</td>
                <td>{{ team.lastname }}</td>
                <td>{{ team.email }}</td>
                <td>
                    <a href="{{ path('app_team_admin_show', {'id': team.id}) }}"><i class="fa-solid fa-eye fa-lg"></i></a>
                    <a href="{{ path('app_team_admin_edit', {'id': team.id}) }}"><i class="fa-solid fa-pen-to-square fa-lg ml-3"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucun résultat</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_team_admin_new') }}" class="btn btn-success btn-lg">Nouvel administrateur</a>
{% endblock %}
