<form method="post" action="{{ path('app_categories_admin_delete', {'id': category.id}) }}" onsubmit="return confirm('Supprimer cette donnée ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="btn btn-danger">Supprimer<i class="fa-solid fa-trash-alt fa-lg ml-3"></i></a>
</form>
