{% extends 'base.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block body %}
<div class="d-flex justify-content-end">
    <form method="post" action="{{ path('delete_account', {'id': app.user.id}) }}" onsubmit="return confirm('Vous voulez vraiment nous quitter ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ app.user.id) }}">
        <button class="btn btn-danger">Supprimer mon compte</a>
    </form>
</div>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ path('account_dashboard') }}" class="btn btn-info">Annuler</a>   <button class="btn btn-success">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}

{% endblock %}
